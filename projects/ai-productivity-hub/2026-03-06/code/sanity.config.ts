import { defineConfig } from 'sanity'\nimport { deskTool } from 'sanity/desk'\nimport { visionTool } from '@sanity/vision'\n\nimport { schemaTypes } from './sanity/schemaTypes'\n\n/**\n * Sanity Studio config for AI Productivity Hub\n *\n * Expected env vars (set in your hosting provider / local .env):\n * - NEXT_PUBLIC_SANITY_PROJECT_ID\n * - NEXT_PUBLIC_SANITY_DATASET\n * - SANITY_API_VERSION (optional, defaults below)\n */\nconst projectId = process.env.NEXT_PUBLIC_SANITY_PROJECT_ID\nconst dataset = process.env.NEXT_PUBLIC_SANITY_DATASET\n\nif (!projectId) throw new Error('Missing env: NEXT_PUBLIC_SANITY_PROJECT_ID')\nif (!dataset) throw new Error('Missing env: NEXT_PUBLIC_SANITY_DATASET')\n\nconst apiVersion = process.env.SANITY_API_VERSION || '2025-01-01'\n\nexport default defineConfig({\n  name: 'ai-productivity-hub',\n  title: 'AI Productivity Hub',\n  projectId,\n  dataset,\n  apiVersion,\n\n  // If you mount Studio inside Next.js (recommended), this should match the route.\n  basePath: '/studio',\n\n  plugins: [deskTool(), visionTool()],\n\n  schema: {\n    types: schemaTypes,\n  },\n})
